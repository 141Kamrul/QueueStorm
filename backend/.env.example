# Environment variables for the QueueStorm Warmup backend.
# Copy this file to `.env` and adjust as needed.

# Port to bind (Render / Railway / Fly set this automatically).
PORT=8000

# Comma-separated list of allowed CORS origins. Use "*" for the mock round.
CORS_ORIGINS=*