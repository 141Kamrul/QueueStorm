# Backend API base URL.
# Vite inlines any var prefixed with VITE_ into the client bundle at build time.
# When unset, requests go to /api (proxied to http://localhost:8000 in dev,
# or resolved on the same origin when served by the FastAPI backend).
VITE_API_BASE=https://queue-storm-7vjq.vercel.app
